# Log

What changed and why, oldest first. One line per change, appended as the wiki
grows. `git log` has the exact diffs; this is the readable version.

- Wiki created. Seeded [[index]] and [[schema]].
