/* eslint-disable no-restricted-syntax */
// Electron-specific color constants
// These values must match src/styles/colors.ts design tokens
// This file exists because Electron's native APIs require hex strings

export const electronColors = {
  // Dark theme background (default for native window)
  backgroundPrimary: "#1a1b1e",
} as const;
